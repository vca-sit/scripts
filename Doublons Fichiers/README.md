Ce script utilise python il faut donc que python soit installé sur la machine
ensuite il utilise en entrée dans le meme dossier un fichier genere par cette
commande:
# fdupes -R /share/VCA/DGSRV/ -G 20000000 -S -l -t > doublons.txt
il faut aussi installer la dependance suivante
# pip install openpyxl
